{
  "version": 2,
  "installCommand": "pip install -r requirements.txt --break-system-packages",
  "buildCommand": "reflex export --frontend-only --no-zip",
  "outputDirectory": ".web/_static",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
